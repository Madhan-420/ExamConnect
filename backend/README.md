# Exam Connect Backend - FastAPI

## Setup
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

## API Docs
Open `http://localhost:8000/docs` for interactive API documentation.
